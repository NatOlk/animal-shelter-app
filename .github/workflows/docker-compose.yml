name: 🐾 Animal Shelter Docker Compose (without nginx)

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  test-compose:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🛠 Install Docker Compose
        run: sudo apt-get update && sudo apt-get install -y docker-compose

      - name: 🐳 Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 🚀 Start services (without nginx)
        run: docker-compose up -d redis-ansh zookeeper-ansh kafka-ansh postgres-animal-shelter postgres-animal-shelter-notification ansh-service ansh-notification-service ansh-react-app

      - name: 📜 Show all running containers
        run: docker ps -a

      - name: 🔍 Check exposed ports
        run: docker ps --format "{{.Names}} - {{.Ports}}"

      - name: 📜 Show logs from ansh-service
        run: docker logs ansh-service --tail 50 || echo "No logs found"

      - name: 🔍 Check if ansh-service is actually listening inside container
        run: docker exec ansh-service netstat -tulnp || echo "No open ports found inside ansh-service"

      - name: 🔍 Test HTTPS with curl
        run: curl -k --retry 5 --retry-delay 5 https://localhost:8443 || echo "ansh-service is not responding on HTTPS"

      - name: 🔍 Check if 8443 is accessible from outside
        run: nc -zv localhost 8443 || echo "Port 8443 is not open"

      - name: 🐳 Test port 8443 inside ansh-service
        run: docker exec ansh-service nc -zv 127.0.0.1 8443 || echo "Port 8443 is not open inside ansh-service"

      - name: 🚀 Check if services are running
        run: |
          curl -k --retry 5 --retry-delay 5 https://localhost:8443 || (echo "ansh-service is not responding on HTTPS" && exit 1)
          curl -k --retry 5 --retry-delay 5 http://localhost:8444 || (echo "ansh-notification-service is not running" && exit 1)

      - name: 🛑 Stop containers
        run: docker-compose down
